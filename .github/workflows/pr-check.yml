name: PR Check

on:
    pull_request:
        branches: [main]


jobs:
    pr-check:
        runs-on: ubuntu-latest
        steps:

            - name: PR check is runnning
              run: echo "PR check running for branch ${{ github.ref_name }}"
    